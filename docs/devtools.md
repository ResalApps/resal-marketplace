# Devtools

Developer productivity tools for Resal engineering workflows.

## Table of Contents

- [Overview](#overview)
- [Skills](#skills)
  - [pr-review](#pr-review)
  - [how-to-test](#how-to-test)
  - [resal-standards-review](#resal-standards-review)
  - [pr-generate-description](#pr-generate-description)
- [Installation](#installation)
- [How to Use](#how-to-use)
- [Workflow](#workflow)
- [Bundled Files](#bundled-files)
- [Example Session](#example-session)

---

## Overview

| | |
|---|---|
| **Plugin name** | `devtools` |
| **Bundled skills** | 4 |
| **Skill names** | `pr-review`, `how-to-test`, `resal-standards-review`, `pr-generate-description` |
| **Slash commands** | `/devtools:pr-review`, `/devtools:how-to-test`, `/devtools:resal-standards-review`, `/devtools:pr-generate-description` |
| **Triggers on** | "address PR comments", "process review feedback", "resolve PR threads"; "generate a how-to-test guide", "QA test manual", "how do I test this feature"; "review against Resal standards", "compliance report", "standards audit", "gaps report", "remediation plan"; "generate a detailed PR", "write the PR description", "document this feature for review" |

## Skills

### pr-review

Processes review feedback on a GitHub pull request. The skill gathers unresolved review threads, classifies each comment as valid, invalid, or needing user input, presents a plan, applies approved fixes, posts replies where needed, pushes changes, and resolves addressed threads.

Use it when you have a PR URL, PR number, or `owner/repo#number` and want to systematically handle reviewer feedback before merging.

The skill remains available outside Spec Kit. Inside Spec Kit, use the equivalent `pr-review`
extension command `/speckit-pr-review-process`.

### how-to-test

Produces (or updates) an internal, QA-facing **How-To-Test manual** for a completed feature — treat it as part of "done", alongside the full-suite run and the PR. The manual is a self-contained HTML walkthrough that a human tester can follow by hand to validate every user case; it does not replace automated tests.

It scans the workspace to detect project structure (single- or multi-project), creates or refreshes `.github/memory/project-memory.md`, then writes a self-contained HTML manual plus a table-of-contents `index.html` for each impacted project:

- **Web frontends** (Vite / Next.js / CRA) — manual under `<public-dir>/how-to-test/features/<parent-feature>/`, with the whole `how-to-test/` directory stripped from production builds and any in-app link gated on a DEV flag.
- **Mobile frontends** (React Native / Expo) — manual under `<project-root>/how-to-test/features/<parent-feature>/`, with mobile screenshots captured through the existing mobile E2E runner when available.
- **Generic projects** — manual under `<project-root>/how-to-test/features/<parent-feature>/`.
- **Multi-project workspaces** — one manual per impacted project plus a workspace-level aggregating index.

Each manual contains a cover + TOC (marked development-only), prerequisites/dev tooling, generated architecture/process-flow visuals when the feature changed architecture or workflow behavior, one plain-English section per user story with real-life scenarios and expected results, screenshots for every form/page/dialog/menu/state, and request/response samples for headless API endpoints or mobile-inaccessible flows. Screenshots are **generated by a Playwright or mobile E2E spec that mocks the feature's backend endpoints** (deterministic) — never hand-taken — and the HTML is held to an a11y smoke check.

When architecture or process flow is part of the implementation, the skill uses the
`architecture-diagram` and `process-flow-diagram` skills to create source HTML diagrams, export PNG
images, and embed/reference those visuals in the How-To-Test manual.

Use it when a feature/spec implementation is complete and you need a QA test guide. Triggers on "generate a how-to-test guide", "QA test manual", "how do I test this feature".

The skill remains available outside Spec Kit. Inside Spec Kit, use the equivalent `how-to-test`
extension command `/speckit-how-to-test-document`, with `/speckit-how-to-test-analyze` for the early readiness
audit.

### resal-standards-review

Reviews/audits any Resal project against the **Resal Engineering Standards** and produces a severity-rated **Compliance & Gaps Report**. It **auto-detects the stack** from marker files and loads the matching rules:

| Marker | Stack | Rules loaded |
|---|---|---|
| `*.csproj` / `*.sln` | .NET | core + dotnet |
| `package.json` with `react-native`/`expo` | React Native | core + react-native |
| `package.json` with `react`/`next` (no RN) | React web | core + react-web |
| `pyproject.toml` / `app/` + FastAPI | Python | core + python |
| more than one marker | multi | core + each stack (per-stack report sections) |

Findings are rated **critical / high / medium / nice-to-have**. Two modes:
- **Full (default):** report → **asks which severity tiers** to include → generates a **Remedy Plan** for those tiers only.
- **Report-only:** say "report only" / "audit only" / "no remedy" to stop at the report (no questions, no plan).

The full prose standards (`core.md` + per-stack files) are bundled under `skills/resal-standards-review/standards/`; the skill's `checks-*.md` catalogs are self-sufficient even without them. The skill never edits target code — the remedy plan is a written plan unless you explicitly ask it to execute.

### pr-generate-description

Wraps up a finished feature by producing two reviewer-facing documents and filling in the pull
request description. It writes:

- `docs/<feature-slug>/CHANGELOG.md` — a technical, Keep-a-Changelog-style record of what shipped.
- `docs/<feature-slug>/<Feature>-Explained.md` — a plain-English, product-manager-friendly narrative
  of the feature, its purpose, the scenarios it supports with concrete examples, and generated
  architecture/process-flow visuals when relevant.

It then creates or updates the PR for the current branch, injecting the feature-details narrative
under the heading **"What have been developed and how to review it"** between idempotency markers, so
re-running refreshes the section instead of duplicating it.

| Behavior | Detail |
|---|---|
| **Feature detection** | Reads `.specify/feature.json` in Spec Kit projects; falls back to the git branch / a `docs\|specs` folder elsewhere, and asks if ambiguous. |
| **PR handling** | Updates an existing PR; offers to create one (`--create-pr` to skip the prompt). `--no-pr` writes docs only. |
| **Grounding** | Reads `spec.md`/`plan.md`/`data-model.md`/`tasks.md`/`git log`; never fabricates test counts, coverage, or issue numbers. |
| **Diagram assets** | Uses `architecture-diagram` and `process-flow-diagram` when implementation changes need architecture or workflow visuals, writing HTML + PNG files under `docs/<feature-slug>/assets/diagrams/`. |
| **Idempotent** | Fixed doc paths + marker-delimited PR section converge on re-run. |

This skill is the **portable twin of the `pr` Spec Kit extension** hosted in
[`extensions/pr/`](../extensions/) — same name, same behavior. Use the **extension** to wire it into
the Spec Kit `after_implement` lifecycle; use the **skill** for the slash command anywhere.

## Installation

Run this inside Claude Code:

```
/plugin install devtools@resal
```

Then run `/reload-plugins` if the plugin list does not update automatically.

## How to Use

Automatic trigger examples:

| Say this... | Skill activated |
|-------------|-----------------|
| "Address the comments on ResalApps/example#123" | pr-review |
| "Process this PR feedback: https://github.com/owner/repo/pull/123" | pr-review |
| "Resolve the review threads on PR 42" | pr-review |
| "Generate a how-to-test guide for this feature" | how-to-test |
| "Create a QA test manual for the spec feature 1234-plan-catalog" | how-to-test |
| "How do I test this — write the QA walkthrough" | how-to-test |
| "Review this service against the Resal standards" | resal-standards-review |
| "Run a compliance and gaps report on ./ResalPay (report only)" | resal-standards-review |
| "Audit this app against our coding standards and give me a remediation plan" | resal-standards-review |
| "Generate a detailed PR for this feature" | pr-generate-description |
| "Write the PR description and a changelog for this branch" | pr-generate-description |
| "Document this feature for review" | pr-generate-description |

Manual commands:

```
/devtools:pr-review ResalApps/example-repo#123
/devtools:how-to-test                        # generate a QA How-To-Test manual for the current feature
/devtools:resal-standards-review
/devtools:pr-generate-description
/devtools:pr-generate-description --no-pr        # docs only
/devtools:pr-generate-description --create-pr    # also create the PR if none exists
```

## Workflow

1. Gather PR metadata, diffs, review comments, review summaries, issue comments, and review thread IDs through `gh`.
2. Filter out already resolved, outdated, author-authored, or purely status-only comments.
3. Classify each substantive comment as valid, invalid, or needing user input.
4. Present a plan and wait for user approval before editing or posting replies.
5. Apply approved fixes, run targeted verification, commit, and push.
6. Post a summary comment and resolve addressed review threads.

## Bundled Files

```text
plugins/devtools/
+-- .claude-plugin/
|   +-- plugin.json
+-- README.md
+-- skills/
    +-- pr-review/
    |   +-- SKILL.md
    +-- how-to-test/
    |   +-- SKILL.md
    +-- resal-standards-review/
    |   +-- SKILL.md
    |   +-- checks-core.md
    |   +-- checks-python.md
    |   +-- checks-dotnet.md
    |   +-- checks-react-web.md
    |   +-- checks-react-native.md
    |   +-- report-template.md
    |   +-- remedy-plan-template.md
    |   +-- standards/         (bundled prose: core + per-stack)
    +-- pr-generate-description/
        +-- SKILL.md
```

> The `pr-generate-description` skill is mirrored as the **`pr` Spec Kit extension** under
> [`extensions/pr/`](../extensions/) (installed with the `specify` CLI, not `/plugin`).

## Example Session

```
User: Address the comments on ResalApps/resal-marketplace#6
Claude: I will gather the review threads, classify each comment, and show you a fix/reply plan before changing anything.
```
