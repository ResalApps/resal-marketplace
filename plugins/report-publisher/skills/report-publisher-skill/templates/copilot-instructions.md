# GitHub Copilot Instructions — Report Publisher

When asked to publish generated reports, follow the Report Publisher Skill workflow.

Use the shell scripts in `/opt/report-portal/scripts` or remote publishing scripts from a local workstation. Do not publish sensitive reports publicly unless the user explicitly confirms.
